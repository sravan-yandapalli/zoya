/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true, // Disable Next.js image optimization (if needed for your use case)
  },
};

module.exports = nextConfig;
